# Local Ollama is the default and does not require a cloud API key.
OPENAI_API_KEY=ollama
OPENAI_MODEL=llama3:latest
OPENAI_BASE_URL=http://127.0.0.1:11434/v1

# For a cloud provider, replace these values and keep real secrets out of Git.
